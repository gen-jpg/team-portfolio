# Contact form destination (mailto fallback)
NEXT_PUBLIC_CONTACT_EMAIL=hello@studio.example

# Optional Supabase — inquiries insert when both are set
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Intended inquiries table columns (create in Supabase when ready):
# id uuid default gen_random_uuid() primary key
# name text not null
# email text not null
# company text
# project_type text
# message text not null
# created_at timestamptz default now()
